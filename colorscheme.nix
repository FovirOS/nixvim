{...}: {
  # Configure theme.
  colorschemes.catppuccin = {
    enable = true;
    settings = {
      flavour = "mocha";

      integrations = {
        cmp = true;
        mini.enabled = true;
      };
    };
  };
}
